services:
  - type: web
    name: studyos-backend
    env: docker
    region: oregon
    plan: free
    dockerCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: ALLOWED_ORIGINS
        value: "*"
